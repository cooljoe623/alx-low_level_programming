#include <stdio.h>
#include <stdlib.h>
/**
 * main - main where the execution of this program starts
 * Description: Get a random number and check its last digit, compare it with 5
 * Return: 0
 */
int main(void)
{
	char i = 'a';

	while (i <= 'z')
	{
		putchar(i);
		i++;
	}

	putchar('\n');
	return (0);
}
